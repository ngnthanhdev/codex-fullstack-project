# Project Brief

Create a fullstack product with:

- Expo React Native mobile frontend in `apps/mobile`.
- NestJS backend API in `apps/api`.
- Shared planning, API contract, and delivery workflow at the repository root.

Fill this brief with the product idea before implementation starts.

